# Benchmark credibility report — 2026-05-30

> Auto-generated by `make benchmark-credibility-report`.
> Source of truth: discovery store + agent_rankings table.

## Headline

- **0 publishable** out of 2 capabilities. These are the only leaderboards we will defend externally.
- 0 developing.
- 0 smoke test.
- 2 synthetic only.

## Thresholds in effect

- Real-adapter providers per capability: ≥ 3
- Real samples on at least one provider:  ≥ 30
- Run age:                                 ≤ 30 days

Override via environment variables:
`PLANMYAGENTS_CREDIBILITY_MIN_REAL_PROVIDERS`, `PLANMYAGENTS_CREDIBILITY_MIN_SAMPLE_SIZE`, `PLANMYAGENTS_CREDIBILITY_MAX_RUN_AGE_DAYS`.

## SYNTHETIC (2)

### `contact_enrichment`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `contact_enrichment` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `email_verification`

- Real providers: **0** of 10 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `email_verification` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.
