# Benchmark credibility report — 2026-05-10

> Auto-generated by `make benchmark-credibility-report`.
> Source of truth: discovery store + agent_rankings table.

## Headline

- **0 publishable** out of 29 capabilities. These are the only leaderboards we will defend externally.
- 0 developing.
- 0 smoke test.
- 29 synthetic only.

## Thresholds in effect

- Real-adapter providers per capability: ≥ 3
- Real samples on at least one provider:  ≥ 30
- Run age:                                 ≤ 30 days

Override via environment variables:
`PLANMYAGENTS_CREDIBILITY_MIN_REAL_PROVIDERS`, `PLANMYAGENTS_CREDIBILITY_MIN_SAMPLE_SIZE`, `PLANMYAGENTS_CREDIBILITY_MAX_RUN_AGE_DAYS`.

## SYNTHETIC (29)

### `booking_execution`

- Real providers: **0** of 4 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `booking_execution` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `browser_automation`

- Real providers: **0** of 5 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `browser_automation` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `company_data_lookup`

- Real providers: **0** of 9 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `company_data_lookup` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `contact_enrichment`

- Real providers: **0** of 6 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `contact_enrichment` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `cross_border_commerce`

- Real providers: **0** of 4 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `cross_border_commerce` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `data_transformation`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `data_transformation` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `database_query`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `database_query` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `delivery_tracking`

- Real providers: **0** of 3 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `delivery_tracking` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `document_retrieval`

- Real providers: **0** of 3 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `document_retrieval` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `email_verification`

- Real providers: **0** of 2 candidates
- Max real sample size: 0
- Reasons:
  - All 2 ranking row(s) for `email_verification` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `entity_extraction`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `entity_extraction` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `fare_comparison`

- Real providers: **0** of 4 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `fare_comparison` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `flight_status_lookup`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `flight_status_lookup` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `itinerary_planning`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `itinerary_planning` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `lodging_comparison`

- Real providers: **0** of 3 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `lodging_comparison` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `lodging_search`

- Real providers: **0** of 3 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `lodging_search` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `payment_authorization`

- Real providers: **0** of 5 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `payment_authorization` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `receipt_processing`

- Real providers: **0** of 4 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `receipt_processing` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `research_synthesis`

- Real providers: **0** of 3 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `research_synthesis` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `semantic_search`

- Real providers: **0** of 9 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `semantic_search` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `shipping_quote`

- Real providers: **0** of 5 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `shipping_quote` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `structured_data_lookup`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `structured_data_lookup` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `structured_extraction`

- Real providers: **0** of 3 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `structured_extraction` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `travel_search`

- Real providers: **0** of 5 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `travel_search` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `web_fetch`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `web_fetch` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `web_interaction`

- Real providers: **0** of 5 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `web_interaction` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `web_scraping`

- Real providers: **0** of 10 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `web_scraping` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `web_search`

- Real providers: **0** of 7 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `web_search` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.

### `workflow_automation`

- Real providers: **0** of 1 candidates
- Max real sample size: 0
- Reasons:
  - All 0 ranking row(s) for `workflow_automation` use synthetic adapters. The leaderboard scores our scoring code, not the vendors.
- Unblockers:
  - Add at least 3 real provider adapter(s) and run them via `make benchmark-schedule`.
